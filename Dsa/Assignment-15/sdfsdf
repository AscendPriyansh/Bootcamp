#include<bits/stdc++.h>
using namespace std;

bool isPrime(int n) {
    
}

int main() {
    int n;
    cin>>n;

    vector<bool> v(n+1, true);
    v[0] = v[1] = false;

    for(int i=2; i<=n; i++) {
        if(isPrime(i)) {
            for(int j=2*i; j<=n; j+=i) {
                v[j] = false;
            }
        }
    }

    return 0;
}